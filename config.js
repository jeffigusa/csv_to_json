
module.exports = {
    array_indicator: 'index',           // if row 1, col 1 == array_indicator, output JSON as array, else output JSON as object
    nested_col_delimiter: '/',          // treat column as a nested object or array
    nested_value_delimiter: '\:',       // denotes a cell as a nested object {key: value}
    unwanted_filepath_substrings: [

    ]
}